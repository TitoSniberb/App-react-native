import React from 'react';
import { 
    Text,

} from 'react-native';
import { ScreenContainer } from 'react-native-screens';

export const Perfil = ({ navigation }) => (
    <ScreenContainer>
        <Text>Esto es el futuro perfil</Text>
    </ScreenContainer>
)